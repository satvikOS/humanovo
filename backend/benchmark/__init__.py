"""
humanovo benchmark suite.

Modules:
    citation_accuracy          — verify every citation in pipeline output
    retrospective_validation   — run pipeline against 10 validated hypotheses

Results are written to backend/benchmark/results/ (gitignored).
"""
