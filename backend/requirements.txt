# Humanovo Backend Dependencies
# Last updated: 2026-04-14
# Rule: every import in the codebase MUST have a corresponding entry here.

# ─── Web Framework ───────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.12
websockets>=13.0

# ─── Database ────────────────────────────────────────────────────
sqlalchemy>=2.0.35
asyncpg>=0.30.0
alembic>=1.14.0
psycopg2-binary>=2.9.9

# ─── Graph Database ──────────────────────────────────────────────
neo4j>=5.25.0
# py2neo removed — unmaintained since 2021, use neo4j driver directly

# ─── Vector Database (pgvector — PostgreSQL native) ──────────────
pgvector>=0.3.6

# ─── LLM Providers ──────────────────────────────────────────────
# openai SDK — required for AsyncAzureOpenAI + AsyncOpenAI
# The orchestrator uses AsyncAzureOpenAI (added in openai>=1.6.0)
# and model-specific Azure AI endpoints via AsyncOpenAI base_url override
openai>=1.55.0

# boto3 — required for AWS Bedrock (Claude Opus/Sonnet, Cohere Embed v3)
boto3>=1.35.0

# ─── AI/ML Orchestration ────────────────────────────────────────
langchain>=0.3.0
langchain-openai>=0.2.0
langchain-community>=0.3.0
langgraph>=0.2.50
tiktoken>=0.7.0

# ─── Embeddings & Transformers ───────────────────────────────────
sentence-transformers>=3.3.0
transformers>=4.46.0
torch>=2.4.0

# ─── NLP / Biomedical ───────────────────────────────────────────
spacy>=3.7.0
scispacy>=0.5.5
biopython>=1.84

# ─── Data Processing & Scientific Computing ─────────────────────
pandas>=2.2.0
numpy>=1.26.4
scipy>=1.14.0
scikit-learn>=1.5.0

# ─── Medical Imaging ────────────────────────────────────────────
pydicom>=2.4.4
nibabel>=5.3.0
scikit-image>=0.24.0
Pillow>=10.4.0

# ─── Electrophysiology / Signal Processing ──────────────────────
pyedflib>=0.1.38
mne>=1.8.0

# ─── Biomechanics ───────────────────────────────────────────────
c3d>=0.5.2

# ─── Visualization (server-side) ────────────────────────────────
matplotlib>=3.9.0
plotly>=5.24.0
kaleido>=0.2.1

# ─── Task Queue ──────────────────────────────────────────────────
celery>=5.4.0
redis>=5.2.0
flower>=2.0.1

# ─── HTTP / API Clients ─────────────────────────────────────────
httpx>=0.27.0
aiohttp>=3.10.0
beautifulsoup4>=4.12.3
lxml>=5.3.0

# ─── Document Generation ────────────────────────────────────────
reportlab>=4.2.0
python-docx>=1.1.2

# ─── Utilities ───────────────────────────────────────────────────
pydantic>=2.9.0
pydantic-settings>=2.6.0
python-dotenv>=1.0.1
tenacity>=9.0.0
structlog>=24.4.0

# ─── Testing ────────────────────────────────────────────────────
pytest>=8.3.0
pytest-asyncio>=0.24.0
pytest-cov>=5.0.0
pytest-timeout>=2.3.0

# ─── Development ────────────────────────────────────────────────
black>=24.8.0
isort>=5.13.2
mypy>=1.11.0
ruff>=0.6.0
