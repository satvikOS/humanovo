[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "humanovo-backend"
version = "0.2.0"
description = "humanovo — Adversarial Biomedical Hypothesis Engine (backend)"
readme = "README.md"
requires-python = ">=3.11"
license = {text = "Proprietary"}

[tool.black]
line-length = 100
target-version = ['py311']
include = '\.pyi?$'
exclude = '''
/(
    \.eggs
  | \.git
  | \.hg
  | \.mypy_cache
  | \.tox
  | \.venv
  | _build
  | buck-out
  | build
  | dist
  | migrations
)/
'''

[tool.isort]
profile = "black"
line_length = 100
known_first_party = ["app"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "W", "I", "N", "UP", "B", "C4"]
ignore = [
    "E501",   # Line too long (handled by formatter)
    "B008",   # Function call in default argument (FastAPI Depends pattern)
    "B007",   # Loop control variable not used (acceptable for readability)
    "B904",   # Exception chaining (style preference)
    "B905",   # zip without strict (not always necessary)
    "C401",   # Unnecessary generator (minor optimization)
    "C414",   # Unnecessary list call within sorted (minor)
    "C416",   # Unnecessary list comprehension (minor)
    "N805",   # First argument naming (validators use cls)
    "N806",   # Variable in function should be lowercase (SIR model uses S, I, R)
    "E741",   # Ambiguous variable name (I is standard in epidemiology)
    "F841",   # Local variable assigned but never used (will clean up incrementally)
    "E701",   # Multiple statements on one line (colon) — compact severity ladders in clinical/biomech code
    "E702",   # Multiple statements on one line (semicolon) — compact matplotlib axis setup in plot helpers
    "N802",   # Function name should be lowercase (math/clinical helpers like P(), make_D(), reportlab afterFlowable override)
    "N803",   # Argument name should be lowercase (TR, X, R, C0, Vmax, Km — domain-standard scientific naming)
    "N818",   # Exception name should have Error suffix (existing internal markers like BudgetExceeded, PoolExhausted)
]

[tool.mypy]
python_version = "3.11"
warn_return_any = true
warn_unused_configs = true
ignore_missing_imports = true

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
python_files = ["test_*.py"]
python_functions = ["test_*"]
addopts = "-v --tb=short"
