{
  "name": "humanovo-e2e-tauri",
  "version": "0.1.0",
  "private": true,
  "description": "WebdriverIO + tauri-driver harness that drives the PACKAGED humanovo Tauri desktop app (WebView2). Lives outside the browser-based frontend/e2e/ Playwright suite and has its own node_modules so its WebdriverIO deps never collide with the frontend build.",
  "type": "module",
  "scripts": {
    "test": "wdio run ./wdio.conf.ts",
    "test:charts": "wdio run ./wdio.conf.ts --spec ./specs/charts.e2e.ts",
    "typecheck": "tsc --noEmit -p ./tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "20.11.16",
    "@wdio/cli": "9.27.1",
    "@wdio/local-runner": "9.27.1",
    "@wdio/mocha-framework": "9.27.1",
    "@wdio/spec-reporter": "9.27.1",
    "tsx": "4.20.6",
    "typescript": "5.6.3",
    "webdriverio": "9.27.1"
  }
}
